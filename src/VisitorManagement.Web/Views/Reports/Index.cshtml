@model IEnumerable<Visit>
@{
    ViewData["Title"] = "รายงานผู้มาติดต่อ";
    var from = (DateTime)ViewBag.From;
    var to = (DateTime)ViewBag.To;
    var types = (IEnumerable<VisitorType>)ViewBag.Types;
    var departments = (IEnumerable<Department>)ViewBag.Departments;
}

<div class="card vm-card p-3 mb-3">
    <form method="get" class="row g-2 align-items-end">
        <div class="col-md-2">
            <label class="form-label">จาก</label>
            <input type="date" name="from" class="form-control" value="@from.ToString("yyyy-MM-dd")" />
        </div>
        <div class="col-md-2">
            <label class="form-label">ถึง</label>
            <input type="date" name="to" class="form-control" value="@to.ToString("yyyy-MM-dd")" />
        </div>
        <div class="col-md-3">
            <label class="form-label">ประเภท</label>
            <select name="visitorTypeId" class="form-select">
                <option value="">ทั้งหมด</option>
                @foreach (var t in types)
                {
                    <option value="@t.Id" selected="@(ViewBag.VisitorTypeId as int? == t.Id)">@t.Name</option>
                }
            </select>
        </div>
        <div class="col-md-3">
            <label class="form-label">แผนกที่มาหา</label>
            <select name="departmentId" class="form-select">
                <option value="">ทั้งหมด</option>
                @foreach (var d in departments)
                {
                    <option value="@d.Id" selected="@(ViewBag.DepartmentId as int? == d.Id)">@d.Name</option>
                }
            </select>
        </div>
        <div class="col-md-2">
            <button class="btn btn-teal">กรอง</button>
            <a class="btn btn-outline-secondary" asp-action="Csv" asp-route-from="@from.ToString("yyyy-MM-dd")" asp-route-to="@to.ToString("yyyy-MM-dd")">CSV</a>
        </div>
    </form>
</div>

<div class="card vm-card p-3">
    <p class="text-muted">ทั้งหมด @Model.Count() รายการ · เข้าแล้ว @Model.Count(v => v.CheckInAt != null) · ออกแล้ว @Model.Count(v => v.CheckOutAt != null)</p>
    <div class="table-responsive">
        <table class="table">
            <thead><tr><th>รหัส</th><th>ชื่อ</th><th>ประเภท</th><th>มาหา</th><th>เข้า</th><th>ออก</th><th>รถ</th></tr></thead>
            <tbody>
            @foreach (var v in Model)
            {
                <tr>
                    <td>@v.VisitNumber</td>
                    <td>@v.Visitor.FullName</td>
                    <td>@v.VisitorType.Name</td>
                    <td>@v.HostEmployee.FullName</td>
                    <td>@v.CheckInAt?.ToString("dd/MM HH:mm")</td>
                    <td>@v.CheckOutAt?.ToString("dd/MM HH:mm")</td>
                    <td>@v.VehiclePlate</td>
                </tr>
            }
            </tbody>
        </table>
    </div>
</div>
